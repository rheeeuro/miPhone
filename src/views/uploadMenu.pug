extends layouts/main

block content
    .uploadMenu__container
        if loggedUser.id.toString() === admin
            .uploadMenu__btns
                a(href=`/companies${routes.addCompany}`)
                    button 제조사 등록
                a(href=`/phones${routes.upload}`)
                    button 휴대폰 정보 업로드