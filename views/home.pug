extends layouts/main
include mixins/phoneBlock

block content
    .phones
        each item in phones
            +phoneBlock({
                id:item.id,
                name:item.name,
                image:item.imageUrl,
                company:item.company
            }) 